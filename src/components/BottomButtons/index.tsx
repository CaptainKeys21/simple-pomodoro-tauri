import { ConfigButton, MainContainer, StartButton } from './styled';

const BottomButtons = () => {
  return (
    <MainContainer>
      <StartButton>Começar</StartButton>
      <ConfigButton>Configuração</ConfigButton>
    </MainContainer>
  );
};

export default BottomButtons;
